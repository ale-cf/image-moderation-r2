{
  "name": "image-moderation-r2",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that moderates uploaded images with Workers AI, generates 4 transformations, stores them in R2, and serves them via an R2 custom domain.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
